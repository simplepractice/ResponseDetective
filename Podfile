target 'ResponseDetective (iOS)' do
  platform :ios, '8.0'
  use_frameworks!

  target 'ResponseDetective (iOS) Tests' do
    inherit! :search_paths
    pod "Quick", "1.2.0"
    pod "Nimble", "~> 7.0"
  end
end

target 'ResponseDetective (OS X)' do
  platform :osx, '10.10'
  use_frameworks!

  target 'ResponseDetective (OS X) Tests' do
    inherit! :search_paths
    pod "Quick", "1.2.0"
    pod "Nimble", "~> 7.0"
  end
end
